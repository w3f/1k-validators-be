apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  ports:
  - name: backend
    port: {{ .Values.backendPort }}
  - name: metrics
    port: {{ .Values.metricsPort }}
  selector:
    app: {{ .Release.Name }}
