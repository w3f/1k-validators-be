{{- if eq .Values.architecture "microservices" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "otv-backend.telemetry-release-name" . }}

data:
  main.json: {{ .Values.telemetry.config | toPrettyJson }}
{{ end }}  