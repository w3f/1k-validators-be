{{- if eq .Values.architecture "microservices" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "otv-backend.worker-release-name" . }}
  labels:
    app: {{ include "otv-backend.worker-release-name" . }}
spec:
  ports:
  - name: http
    port: {{ .Values.port }}
  selector:
    app: {{ include "otv-backend.worker-release-name" . }}
{{ end }}  